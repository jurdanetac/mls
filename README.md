# frontend
React + TypeScript + Vite

## features wishlist
- Save form state on localStorage so that it persists on page refresh
- Tab-separated text textarea parser
- Basic styling

# backend
Python + Django + Ninja

## features wishlist
- Save the forms on sqlite
- Generate a report on today's properties with over $100k acquisition margin